<div class="mx-auto mt-8 p-6 bg-white rounded-lg shadow-md">
  <h1 class="font-bold text-4xl mb-6 text-gray-800"><%= @user.email %></h1>
  <div class="space-y-4">
    <div>
      <h2 class="text-2xl font-semibold text-gray-700 mb-2">Links</h2>
      <p class="text-xl text-gray-600"><%= @user.links.count %></p>
    </div>
    <div>
      <h2 class="text-2xl font-semibold text-gray-700 mb-2">Comments</h2>
      <p class="text-xl text-gray-600"><%= @user.comments.count %></p>
    </div>
  </div>
</div>