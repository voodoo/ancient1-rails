<%= turbo_stream.prepend "comments" do %>
  <%= render partial: "comments/comment", locals: { comment: @comment } %>
<% end %>

<%= turbo_stream.replace "comment_form" do %>
  <%= render partial: "comments/form", locals: { link: @link, comment: Comment.new } %>
<% end %>